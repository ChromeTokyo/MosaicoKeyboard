# Agent 协作规则

## 项目与优先级

先阅读 `README.md`、`docs/PROJECT_PLAN.md`、`docs/DESIGN_STATUS.md`、`docs/TEAM_PLAN.md` 和 `docs/TASK_BOARD.md`。目标是完整 ESP-Mosaico 的可拆式输入／电池底座，国内嘉立创生产，应用固件最后处理。用户新的明确要求优先于本文件。

当前为 A0 草案，禁止把文件存在、脚本运行成功或库下载成功描述为电气正确、工厂可生产或样机已验证。

## 已指定的负责人

Claude Fable 5.1 主控与总集成；Chrome GPT-6 负责电路／PCB；Cursor 负责机械 CAD；Hiro GPT-6 负责批次独立审查；Grok Bot 负责资料与供应链证据核查。完整职责、模型确认与离线交接规则见 `docs/TEAM_PLAN.md`，以该文件替代早期通用分工。

Hiro 间歇上线，不将其本机或会话作为常驻主控和唯一状态来源。原 A0 由 Hiro 产生，首次独立电气复核交给 Claude。日常工作可以在 Hiro 离线时继续，关键放行仍需实际完成非作者复核。领取任务与执行结果必须落到仓库。

## 并行与文件所有权

- 各任务使用独立分支；并行工作优先使用独立 worktree。
- 每个任务明确负责人、文件范围、依赖与验收条件。本文不自动启动 Agent 或授权外部消息发送。
- 同一 EDA 工程、布局源文件或三维模型同时只允许一个编辑负责人，其他角色通过问题单和审核建议协作。
- 接口变更先更新统一约束，再同步电路、PCB、机械与验收文档。
- 优先通过 PR 集成到主分支。关键电源和接口由非作者复核；不要用自身生成结果代替独立审查。

## 工程事实与证据

- 所有模块引脚、关键尺寸和器件参数注明官方资料、版本和位置；推导和假设单独标记。
- 不从无尺寸照片推算精密制造坐标。未知 Pogo 尺寸不得用常见间距冒充。
- 器件符号、焊盘号和封装按原厂资料核对；供应与装配能力按实际工厂结果确认。
- 给出电源电流、热、逻辑电平及尺寸链的依据；不得静默替换关键器件。
- 新发现的问题及时更新 `docs/DESIGN_STATUS.md`，解决后附证据，保留必要历史。

## 设计文件与生成文件

- 目前 `design/build_design.py` 是实验性生成入口，输出仍需 EDA 验证。
- 不同时手改生成文件和生成脚本。若转为 EDA 原生工程主导，先明确唯一设计源及脚本的保留用途。
- 每个里程碑保留可编辑源文件、输入资料索引、必要审核输出和复现说明。
- 库缓存中未使用或误选的器件不能自动进入生产 BOM。
- 发布制造文件前核对同一版本的原理图、PCB、BOM、贴片坐标、Gerber 和装配图。

## 完成与交接

每次交接说明：改了什么、改动文件、事实依据、完成的验证、未解决问题和下一任务所需输入。无需为文档修改编写形式化测试；硬件设计必须执行对应的电气、机械及制造检查。

未经成功导入与检查，不宣称 EDA 工程可用；未经布线和 DRC，不宣称 PCB 完成；未经实物测试，不宣称硬件验收完成。

用户已创建 GitHub 仓库 `ChromeTokyo/MosaicoKeyboard`。当前没有制造订单；实际下单内容、数量和费用以届时用户指示为准。
