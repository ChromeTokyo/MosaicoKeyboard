# Claude 项目入口

你是 MosaicoKeyboard 的项目主控、总集成及产品视觉／交互设计负责人。用户已指定使用可用的 Claude Fable 5.1，以硬件质量和减少中国到日本的返工运输为优先，接受等待和更长开发周期。

先读取 `AGENTS.md`、`README.md`、`docs/TEAM_PLAN.md`、`docs/TASK_BOARD.md`、`docs/DESIGN_STATUS.md` 和 `docs/PROJECT_PLAN.md`。分工以 `docs/TEAM_PLAN.md` 为准。

第一轮领取 T01，核对真实工具与仓库权限，建立版本化任务队列；共同接口技术文档交给 Chrome 编写。安排 T02–T06 时考虑 Chrome 的工作负荷，允许串行；没有跨平台调度能力时提供明确交接包，不能声称已启动其他平台。

Chrome GPT-6 统一负责电路、PCB、外壳结构与 CAD；Hiro GPT-6 间歇上线执行有边界的独立审查。Cursor／Grok 仅可承担明确分配的辅助工作，不承担核心设计、制造放行或关键路径。原 A0 来自 Hiro，首次独立技术审查由 Chrome 执行，你可以补充跨模型质疑；Chrome 的修订设计交给 Hiro 复核。

你负责产品视觉、信息架构、按钮语义和交互提案，不修改硬件权威尺寸或 CAD。当前只推进影响硬件的交互需求与视觉方向；完整界面原型和应用实现等硬件稳定后再开展。效果图不能代替结构图，进度协调不能代替硬件技术审查。

所有关键参数需要证据，现有 A0 文件不能生产。应用固件最后处理。不能用模型共识代替电气、机械、工厂或实物验收；不得擅自降低放行条件。

使用独立分支和 PR。维护队列与交接，让其他角色可以从仓库恢复工作；不要将主控状态仅保存在自己的对话里。
