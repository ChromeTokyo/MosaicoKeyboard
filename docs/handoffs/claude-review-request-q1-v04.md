# 复核请求：轨一托盘 v0.4 —— 给 Chrome（GPT）与 Grok

由主控起草，2026-09-24。**两位各自独立审，互不通气。** 用户指令：「这次的产出也让 GPT 和 Grok 看看」。

## 0. 为什么这一件要你们看

轨一 v0.1～v0.3 由主控单人闭门迭代、直接发用户，三版全是低级错误，被用户判不合格并指出「没人帮你审」。
v0.4 改为「设计 agent／闸门 agent／两名对抗审核 agent／修正」分开的流程，但**这四个 agent 都是主控派出去的**——
同一上下文、同一套假设。你们是这一件**第一批真正独立**的眼睛。

## 1. 这件东西是什么

轨一（D-031）：用户即将收到 Mosaico 本体 ＋ 官方「Module Interaction」交互模块（一块 PCB ＋ 2×10 排针直插 Mosaico 侧槽 ＋ 两个键盘轴）。
官方模块**悬空挂在排针上**。轨一 = 一个 3D 打印件，**替换官方模块的底板**，把 Mosaico 也定位住，
让排针只走电、不承力；用户到货即打印即用（2 键），同时当轨二的测量夹具。

官方模块解析：`references/official-module-interaction/ANALYSIS.md`。方案 J 权威描述：`review/claude/option-j-seated.md`。

## 2. 文件（分支 `quickstart/v04-r2`，目录 `mechanical/quickstart/`）

| 文件 | 是什么 | SHA-256（前 16） |
| --- | --- | --- |
| `extended_baseplate.scad` | 参数化源；PART = tray / bezel / both | 760827cdf8d656d3 |
| `extended_baseplate.stl` | 托盘 | e9e498e403222c42 |
| `bezel.stl` | 压框 | 9697f9d3a9492d5b |
| `check_quickstart.py` | 机器闸门（与几何**不同 agent** 写） | 44814def9a547fcf |
| `official_plate_fullres.stl` | 官方底板（import 用，一个面未改） | ba96ee93b79ec70c |
| `README.md` | 装配顺序、假设表 Q1-* | — |
| `v04_*.png` | 五视角渲染（iso/top/end/tray_iso/bezel_under） | — |

PR：#66  https://github.com/ChromeTokyo/MosaicoKeyboard/pull/66

闸门实跑输出：`mechanical/quickstart/GATE-OUTPUT-v04r2.txt`（15 条：0 FAIL，3 WARN——螺钉盘头高出屏面 2.7／GAP 0.5 下无止挡／导向裕量 0.095）。
**分支：`quickstart/v04-r2`**（基于 sweep/consistency-20260921）。

## 3. 八条硬要求（闸门逐条断言）

1. 四边保持：Mosaico 四条前缘都被压住，任一角不能抬起；不靠卡扣、不靠 Mosaico 任何边缘特征（圆角／玻璃到边／侧槽全未知）
2. 除压框（厚 ≤1.6）外托盘任何部分 Z ≤ 11.48（屏面）
3. Z ∈ [0, 11.48] 内托盘实体与 Mosaico 包络（45.19 见方 ＋ 2×0.3）相交为空
4. 不碰官方外壳：Z>0 在官方底板脚印内只能有官方底板本身
5. 排针安全靠装配顺序：先落到环形沿（±X 定位）→ 沿针轴推到止挡（此时才进针）→ 压框四颗螺钉。GAP 区 Z∈[2,9] 无实体
6. 可打印：PLA、0.4 喷嘴、壁 ≥1.2、悬空 ≤2、立柱壁 ≥1.5 环绕 Ø1.7
7. Mosaico ±X 面功能区（USB-C＋侧键／顶键＋扬声器，Z≈2～6）中央 34 mm 不被遮挡
8. 假设按 Q1-* 编号登记

## 4. 主控此前犯过的错（**请优先复查这几类**）

| 版 | 错 | 类型 |
| --- | --- | --- |
| v0.1 | U 形框只靠两根 5×2 短桥挂在底板上 | 结构无刚性 |
| v0.2 | 卡舌挂在唇顶上方 2.5 mm 空中 | 特征悬空（Z 基准算错） |
| v0.3 | 立柱放唇内侧，伸进 Mosaico 角落 4.2×4.5 | 伸进被约束件脚印 |
| v0.3 | 压条本体盖到 Mosaico 顶面上方 | 与被约束件顶面干涉 |
| v0.3 | 保持只有远端一根 0.5 薄唇，模块端无约束 → 可绕排针翘起掰针 | 保持不闭合 |
| v0.3 | OpenSCAD 变量先用后定义（占位赋值）→ undef | 语言坑 |

## 4b. 内部两名对抗审核对 v0.4 首版的判决（r2 已按此修，供你们核「修对了没」）

| 项 | 首版问题 | r2 处置 |
| --- | --- | --- |
| **方向** | 排针出口边认错 90°，Mosaico 放在螺柱边 | `rotate(−90)`；闸门 #0 由底板本体内缩＋外壳开口独立判 |
| 压框排针侧边 | 倒角后顶宽 0.73 < 1.2 | 该边不倒角 |
| 夹紧 | 柱顶低于压框底 0.3，需 325 N/颗才到止点，全进玻璃边；实测厚 ≤11.18 则四边全松 | 柱顶 = 压框本体底（真止点），压边处让一层 0.2 游隙；闸门报 MOS_T 可用窗口 [11.30, 11.65] |
| 导向 | 先于触针 0.095，CLR 0.3 下偏摆 2.47°/1.95 mm | CLR 0.15 + 1.5 导入斜角 → 1.55°/1.22 mm（< 半针距 1.27）；仍 WARN |
| 环形沿 | 远端只托 16% | 四边闭合 |
| 外壳高度 | 按 Z=0 起 10.9 | 按底板台阶 1.2 起，顶 12.1 |
| GAP 容差 | 方向写反 | 实际 GAP ∈ [0, 1.3]，变大失效 |
| 闸门 | 与 scad 共用方向假设，互证无效；不查运动学、间隙、显示区 | 15 条，新增 #0/#11/#12/#13/#14 |

## 5. 请你们回答的

1. **闸门抓得住上表的错吗？** 把 v0.3 的 STL 喂给 `check_quickstart.py`，它应当红。不红 = 闸门有洞，洞在哪。
2. **v0.4 的保持真的闭合吗？** 用截面／包围盒亲自量：四角抬起的自由度、压框压边量、立柱位置是否全在 Mosaico 脚印外。
3. **排针**：按 README 的装配顺序做一遍思想实验，哪一步可能横向加载排针；取出顺序对不对。
4. **可打印**：最薄壁、最长悬空、压框 1.6 厚跨 45 mm 的翘曲风险、立柱 Ø1.7 底孔在 0.4 喷嘴下实际会打成多大。
5. **官方件**：托盘会不会挡官方外壳卡合、挡键盘轴、挡排针出口（官方外壳朝 Mosaico 一侧壁的底边有约 24 mm 出口）。
6. **主控没想到的**——这一条最值钱。

## 6. 结论格式

```
结论：PASS / CHANGES_REQUIRED / FAIL
闸门对 v0.3：红 / 绿（绿即闸门有洞）
逐条硬伤：位置、数值、后果、改法
闸门漏洞：
我没核到的：
```

放到你自己的分支 `review/<你>/Q1-v04/REPORT.md`，开 PR。不改设计源。
