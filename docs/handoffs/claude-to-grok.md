提案 · 未冻结 · 由 Claude 主控（claude-opus-5）起草 · 不得据以制造

# 交接包：Grok R0 设计提案首次独立复核

> 本文件是主控派给 Grok 的审查包，Grok 只读。Grok 的领取登记、进展日志与完成交接写在 `docs/handoffs/grok.md`（该文件唯一归属 Grok，由 Grok 自己创建与维护，主控不写入、不代建、不代填；归属规则与 `docs/DECISIONS.md` 补记 D-006 对 `docs/handoffs/chrome.md` 的裁决相同）。审查报告写 `review/grok/R0/REPORT.md`。配套资料索引见 `review/grok/R0/MATERIALS.md`。

- 任务 ID：**R0 设计提案首次独立复核**。这是一次专项复核，不占用 `docs/TEAM_PLAN.md` 第 4 节的 H1–H4 编号，也不替代其中任何一项。
- 派发人：Claude 主控，实际模型 `claude-opus-5`。
- 负责人／角色：Grok（4.6），**独立复核者，只读**。
- 平台、实际模型 ID、推理档位：派发方为 Claude Code，实际模型 `claude-opus-5`；推理档位未由可核验接口暴露，不猜填。**Grok 侧的平台、实际模型 ID 与推理档位由 Grok 在 `review/grok/R0/REPORT.md` 抬头与 `docs/handoffs/grok.md` 自行登记，本文件不代填。** 登记时必须同时写明**如何核验的**：不接受「按订阅应当是某型号」「界面标题写着某型号」这类推断。可参考的做法是从本会话自身的运行记录或请求元数据中读出模型与推理档位字段，只登记字段值与读取时刻，不上传会话原文；平台若确实不暴露，就写「未由可核验接口暴露」并说明尝试过哪些途径，不填猜测值。`docs/TEAM_PLAN.md` 第 1 节要求记录实际模型 ID 与推理档位，本项是硬性要求，缺失即视为抬头不完整。
- 日期：2026-09-21。
- 状态：READY（待 Grok 领取）。本文件写入仓库只表示审查包可领取，不表示 Grok 已上线或已开始工作（`docs/DECISIONS.md` D-003）。
- 输入提交号及接口版本：**冻结提交号在领取时执行 `git fetch origin --prune && git rev-parse origin/main`，把结果登记进报告抬头与进展日志。** 撰写本包时 `origin/main` 为 `efa9739`，该值仅供对照，**不是冻结值**。接口版本：`hardware/ICD-0.2-DRAFT.md` 为 0.2 草案，未冻结；`docs/INTERFACE_CONTROL.md` 是 V1.0 四触点时期的历史草案，仅文首加了适用范围警示，**不是本次审查对象**。
- 输出分支、提交号、PR：由 Grok 领取后填写。建议分支名 `grok/r0-design-review`；关机前必须 push 并开 PR，报告中注明复核过的 `main` 提交号（`docs/TEAM_PLAN.md` 第 4、5 节）。
- 改动文件及源文件／生成文件关系：Grok 可写 `review/grok/R0/REPORT.md`（新建）与 `docs/handoffs/grok.md`（新建，归 Grok）。其余一律只读，见第 1 节。本任务无生成文件。`docs/TASK_BOARD.md` 的 R0 登记行属主控的队列维护职责，Grok 不需要改任务板。
- 结论取值：`PASS`／`CHANGES_REQUIRED`／`BLOCKED`。**证据不全不得 `PASS`。**

## 0. 本包的性质与读法

1. **审查对象全部由主控起草，而本包也由主控撰写。** 因此下文每一处「主控当前判断」都是**待检验的主张，不是审查输入**，写在这里是为了给你一个明确的反驳靶子，不是为了让你确认它。凡本包与源码、原厂资料冲突之处，以源码与原厂资料为准，并在报告中指出本包错在哪里。
2. 三项范围可分别给结论，再给一个总结论。`PASS` 在本次的含义是「该项提案经独立查证在文档与源码层面自洽、可作为下一步投入的基础」，**不是硬件放行**，不是 G1／G2／G3 通过，也不代表实物可用。
3. 每条结论标注「**实际检查**／**推导**／**未检查**」，并引用到具体文件与行号。没有行号的结论按「推导」处理。
4. 本包不含任何机械或电气数值的冻结请求。凡本包出现的数值（针号、偏移、编号）都是引用来源原文，供定位用，不是设计值。
5. **本包刻意不给出任何结论性提示。** 第 3 节的复算题有确定答案，但答案不在本包里，也不在 `review/grok/R0/MATERIALS.md` 里；被审文件中可能出现同样的数字，那是**被审对象的自述**，抄它不算完成复算（见第 3 节规则）。
6. 你与主控结论不一致时，不需要迁就本包的措辞。写清你的推导链与出处即可。

## 1. 本次定位与授权边界

### 1.1 你是什么

**独立复核者，只读。** 本次不请你做设计，不请你补文档，不请你改代码。原因很直接：现在缺的不是第四个产出源，而是一个没有参与起草的人来查这批产出。再加一个无人审的产出源没有价值。

### 1.2 为什么现在需要这次复核（独立性真空）

- Chrome（Codex／`gpt-6-astra`）已耗尽额度下线，硬件技术判定权空缺。
- Hiro（`gpt-6-astra`）完成 H0 后下线。**H0 的范围只有三项，且其报告 §9 明确写明：本次同时合入的 `firmware/`、`hardware/eeprom/` 与其他文档增量不在 H0 范围，不能借该报告宣称获 Hiro 审核。** 本次审查对象正是那批增量。
- 主控（Claude）既是这批设计提案的作者，又是当前唯一持续在线者。按 `docs/DECISIONS.md` D-013（谁设计谁不得担任该次独立审查人）、D-015 与 D-018（主控模型两次切换但同会话、共享上下文，自核仍属同上下文核验），**主控自核不构成独立复核**。
- 结果是：本次审查对象**从未被任何独立方审查过**。这是本任务存在的全部理由。

### 1.3 写入范围（硬性）

你**不得编辑**：`design/`、`hardware/`、`firmware/`、`mechanical/`、`docs/` 下除 `docs/handoffs/grok.md` 以外的任何文件、`review/` 下他人目录（`review/chrome/`、`review/claude/`、`review/hiro/`）。对这些文件的修改意见**全部写进报告**，由主控按结论处理。不动 `main`，不 force push。

### 1.4 分工条款与本次的关系（必须在报告中确认知悉）

- `docs/TEAM_PLAN.md` 第 1 节与第 2 节责任表把 **Cursor／Grok 限定为「可选辅助、默认无核心任务、不作为关键路径依赖」**，且写明「不自动把电路、PCB、CAD 或验收任务转给 Cursor／Grok」「不得自动让 Cursor／Grok 代签」。**本次参与属用户明确批准的例外，仅限本次独立复核，不改变该条款**，也不使 Grok 成为后续任何环节的默认承担者。本次结论不替代 `docs/TEAM_PLAN.md` 第 4 节规定的 Hiro 硬件批次复核，不构成 G1–G3 的任何放行，不批准制造。
- `docs/TEAM_PLAN.md` 第 2 节末另有一条：**「辅助资料不能直接进入冻结设计，不用模型投票决定工程正确性。」本次不以多数决定技术结论。** 你与主控结论相同不等于主控对，不同也不等于主控错；判定依据是源码、原厂资料与可复算的过程，不是「几个模型这么说」。报告里不要写「与主控一致故通过」这类表述。
- `docs/TEAM_PLAN.md` 第 5 节：进展同步是强制项。领取时与关机前各在 `docs/handoffs/grok.md` 的进展日志追加一行。无法完成时写清已查范围与未查范围，不留空白。

## 2. 审查范围（三项）

三项可以按任意顺序做。若额度不足以做完三项，**优先做 (a) 与第 3 节的五道复算题**——那两部分是可完全从源码判定的，价值密度最高；(b) 次之；(c) 最可后置。做不完的部分在报告中明确列为「未检查」，不要用推测填补。

### 2.1 范围 (a)：`hardware/eeprom/` 身份镜像与烧写路径

**审查对象文件：** `hardware/eeprom/mosaico_eeprom_v1.py`、`hardware/eeprom/IDENTITY.md`、`hardware/eeprom/PROGRAMMING.md`、`hardware/eeprom/eeprom_program_example.c`、`hardware/eeprom/sample_handle.bin`、`hardware/eeprom/sample_handle_image.h`、`hardware/eeprom/README.md`、`hardware/eeprom/SHA256SUMS`、`hardware/eeprom/SERIALS.csv`。

**要回答什么：**

1. **镜像格式与 BSP 解析器是否一致。** 工具生成的镜像布局（各字段偏移、宽度、字节序、填充、总长）与固定基线中模块管理器读取并解析描述符的实现是否逐项对应？有没有字段在工具里存在而解析器不读、或解析器要求而工具没写？主机一次读多少字节、从哪个内部地址起读，与镜像文件长度是否匹配？
2. **CRC 算法与分段是否正确。** 校验和的参数、覆盖范围划分、存放位置与字节序，是否与解析器的实现一致？分段边界有没有遗漏字节或重复覆盖？（复算要求见第 3 节 Q1、Q2。）
3. **`IDENTITY.md` 的身份取值是否与 BSP 枚举相容。** 所选 board_type 是否落在管理器的类型枚举内、是否与官方已占用的类型冲突；vendor_id／board_id／版本字段是 BSP 校验的还是本项目自行约定的；`IDENTITY.md` 对「BSP 校验什么、不校验什么」的断言是否与源码相符；与官方同类型驱动共存时的对策（认领前后各做一次核对）在源码语义上是否真的成立。
4. **烧写路径引用的 API 是否真实存在。** `eeprom_program_example.c` 与 `PROGRAMMING.md` 引用的每一个 BSP／ESP-IDF 函数、宏、类型、Kconfig 符号，是否在固定基线（BSP）与所声明的 IDF 版本中真实存在且签名一致？有没有把不存在的符号当作存在、或把某版本才有的 API 当作通用？`PROGRAMMING.md` 对器件页写、写周期、地址脚与写保护脚的描述是否与其所引用的器件资料自洽？其中已自述与模块板分支存在冲突的一处（写保护脚默认态），处理方式是否可接受？
5. **样例镜像能否被 BSP 的解析规则接受。** 直接读 `sample_handle.bin` 的字节，按你从源码读出的规则独立判定：magic、各段校验和、长度字段是否都能通过？`SHA256SUMS` 与 `sample_handle_image.h` 是否与该二进制一致？

**依据在哪：** 固定基线 BSP 源码（`review/chrome/D1-module-interface/evidence/bsp/`，对应官方提交 `392860b1d1a123c3377947074b2af1f600e86c5d`），尤其是模块管理器的头文件与实现、以及板级子板初始化实现。索引见 `review/grok/R0/MATERIALS.md`。

**主控当前判断（供反驳）：** 镜像布局、分段边界、校验算法与存放字节序均与解析器一致；样例镜像可通过解析器的全部校验；身份取值与枚举相容且不与官方量产模块冲突（此条含一条未证的假设）；烧写示例引用的 API 已逐一在基线源码中核到真实名称与签名。**以上每一条都可能是错的，请独立判定。**

**硬性要求：** 从固定基线源码自行复算，**不得采信主控的自测结论、`README.md` 贴出的运行输出、或工具 `selftest` 的通过与否**。工具自测通过只证明工具与自己一致，不证明工具与 BSP 一致。

### 2.2 范围 (b)：`firmware/dock_handle/` 主机侧驱动

**审查对象文件：** `firmware/dock_handle/dock_handle.c`、`firmware/dock_handle/include/dock_handle.h`、`firmware/dock_handle/include/dock_handle_pinmap.h`、`firmware/dock_handle/examples/keytest/`（全部文件）、`firmware/dock_handle/Kconfig`、`firmware/dock_handle/CMakeLists.txt`、`firmware/dock_handle/idf_component.yml`、`firmware/dock_handle/README.md`、`firmware/dock_handle/PC_LINK.md`。

**要回答什么：**

1. **引用的 BSP／IDF 符号是否真实存在于固定提交。** 逐个核对，包括函数名、参数个数与类型、返回类型、枚举成员名、结构体字段名、宏名。`README.md` 第 6 节自称是「API 核实表」，第 6.5 节还列了「核实为不存在」的符号——**这两部分本身就是审查对象**：表里声称存在的是否都存在、行号是否指向正确位置、声称不存在的是否真的不存在。
2. **认领流程与模块管理器语义是否相符。** 认领的前置条件、超时语义、所有权归属、订阅回调的执行上下文与持锁情况、反初始化时的取消订阅与任务退出顺序，是否与管理器实现一致？有没有在回调里做了不该做的事，或假设了管理器并不保证的时序？
3. **`dock_handle_pinmap.h` 与 `hardware/module-board/PINMAP.md` 是否一致。** 后者在分支 `origin/claude/design-d/module-board`，不在 `main`；取法：`git fetch origin && git show origin/claude/design-d/module-board:hardware/module-board/PINMAP.md`。逐键核对，并同时核对 `README.md` 第 4 节转抄的表与头文件实际宏值是否一致（文档与代码是两处，可能各自漂移）。
4. **按键读取方式、热插拔释放路径有无逻辑缺陷。** 引脚配置方式与方向、上拉与读取极性、轮询周期与去抖计数的相互关系与最坏延迟、事件队列满时的行为、状态位图与事件的一致性；拔出时补发释放事件、复位引脚、释放租约、投递断开事件这一串动作的顺序与竞态；自动重认领路径下会不会重复释放、遗漏释放或在已释放的槽上继续读引脚；初始化失败路径上的资源回收是否完整。**这一项请按普通代码审查做，不要只对照文档。**
5. **`Kconfig` 与构建配置是否自洽。** 选项默认值与代码中的使用是否匹配、范围约束是否有效、示例工程的 `sdkconfig.defaults` 与组件依赖声明是否指向同一基线。
6. **`PC_LINK.md`** 作为「第一版建议、不做实现」的文档，其中被当作事实陈述的部分（尤其第 3 节关于某组 Kconfig 符号是否存在的核实结果）是否属实。

**依据在哪：** 同 (a) 的固定基线；另需 `origin/claude/design-d/module-board:hardware/module-board/PINMAP.md`。

**主控当前判断（供反驳）：** 引用的符号全部真实存在；认领与释放顺序与管理器语义相符；引脚表已按 PINMAP.md 改正（上一轮 WIP 曾顺排 H2 针序，与 PINMAP 不一致，README 第 4 节列了改动）；按键为纯轮询、无中断通路；组件未在 ESP-IDF 中编译，只做了宿主机语法检查，这一点已自述为已知限制。**未编译这一事实本身就意味着符号与签名错误不会被工具挡住，请把它当作高风险区。**

### 2.3 范围 (c)：`hardware/ICD-0.2-DRAFT.md` ＋ `hardware/ASSUMPTIONS.md` ＋ `hardware/G6-TEST-PLAN.md`

**要回答什么：**

1. **弹簧针 2×8 分配的防呆论证是否成立。** ICD 第 3.2 节给出逐针分配，第 3.3 节按三级论证防呆，其中二级（电气分配）用一张表枚举了三种错位情形：**沿列方向错位一列（两个方向）、错行、绕轴 180°**。请独立重做这个枚举：按第 3.2 节的分配表，逐针推导每种错位下「底座针 → 模块板焊盘」的实际映射，判断表中每一行的结论是否成立；特别注意枚举是否完整（列出的三类是否覆盖了所有可几何实现的错位，有没有被漏掉的组合），以及「无损」的判断是否在每一针上都真的成立。论证若依赖某一级防呆生效，请指出该依赖是否被显式写出。
2. **假设总表是否覆盖设计中实际使用的全部假设。** 反向核对：在 `hardware/ICD-0.2-DRAFT.md`、`hardware/eeprom/`、`firmware/dock_handle/` 里搜 `ASSUMPTION`，看每一处引用的编号是否在 `hardware/ASSUMPTIONS.md` 中有对应条目；再正向看，设计文档中有没有实际用了某个未标注的假设值（被当成事实写的数值或结论）。各子系统分支自行编号的临时假设（`AS-31-*`）与总表的衔接是否说明清楚。
3. **每条假设是否有可执行的验证步骤。** 判据是「拿到实物的人照着做能得到是／否的结论」：步骤是否指明测什么、用什么器材、在什么状态下测、读数如何判定。**步骤所述的测量条件、器材与被测量三者是否互相匹配**；器材清单与步骤是否对得上（步骤要求的仪器有没有漏在清单外、或清单里的器材无人使用）。执行者被假定不具备工程调试能力（`docs/TEAM_PLAN.md` 第 6 节），步骤须对该读者可独立执行。
4. **G6 计划的测试项与阈值登记是否自洽。** 逐项核对：每个测试项引用的阈值编号是否在第 2 节阈值登记表中存在；反过来，登记表中的每个阈值是否至少被一个测试项使用。**重点：第 2 节的阈值当前几乎全为「待定义」——请检查测试项的判定语句在阈值未定义时会发生什么**，是否存在某一项在阈值空缺的情况下仍能被判为通过（隐含放行），或者反过来整条链条因此无法执行却未被标为阻塞。故障树与判定规则是否与测试项编号对应。
5. ICD 第 0 节的归属与状态标签、第 9 节网络命名总表，与其他两份文件及 `hardware/eeprom/`、`firmware/dock_handle/` 使用的网络名是否一致。

**依据在哪：** 三份文件互相之间，以及它们与 `hardware/eeprom/`、`firmware/dock_handle/` 的引用关系。本项主要是内部自洽性与可执行性审查，不要求你判定任何电气数值的正确性（那属于 Chrome 与 Hiro 的职责，且多数数值目前是 unknown）。

**主控当前判断（供反驳）：** 三种错位情形下的电气后果均为「无损、功能错误可辨认」，绕轴 180° 依赖一级机械防呆且该依赖已写明；假设总表覆盖了 ICD 与两个子系统的全部假设；G6 阈值待 Chrome 定义是已知空缺，不构成放行漏洞。

## 3. 校准与复算要求（本次的关键节）

下面五道题**有确定答案**。请在报告中**逐题给出你自己的独立复算过程与结果**。

**规则：**

- **每题必须给出源码文件与行号。** 没有行号的答案不计为完成。
- **不得引用主控或 Chrome 的结论作为依据。** 这包括：`hardware/` 与 `firmware/` 下被审文件里的自述数值、`review/chrome/D1-module-interface/` 下的分析文档、`review/claude/` 下的任何文件。这些文件你可以读（它们是审查对象或背景），但**它们的数字不能充当你的答案来源**；答案必须从固定基线 BSP 源码、官方引脚表原文、以及被审的二进制／脚本本身导出。被审文件里若出现与你结论相同的数字，那只说明它自述如此，请在报告中把两者分开写：「我从源码得到 X（出处：文件:行号）；被审文件 Y 自述为 X，一致／不一致」。
- 五题只考察从源码与原始资料的复算能力，**不是**要你猜主控想要什么答案。
- 答错某题不影响你其他发现的采纳；但会影响主控对你**未附复算过程**的那些结论的采信权重。这条规则写在这里，是为了让你知道这次评估的规则是公开的。

| 题号 | 要求 |
| --- | --- |
| **Q1** | 从 BSP **解析器源码**推导 EEPROM 镜像的分段边界与各段字节数，并给出镜像总长。要求：写出每一段的起止偏移、该段字节数、以及该段校验和存放在哪里；再说明总长是怎么由这些部分加出来的。出处给到解析函数的文件与行号。 |
| **Q2** | 从源码读出校验算法的**初值、多项式、移位方向、是否反射、有无最终异或**五项参数；用你读出的这组参数**独立计算** ASCII 字符串 `123456789` 的校验值（给出你实际用的计算方式，例如手算步骤或你运行的代码）；并说明镜像中该校验值以何种字节序存放。出处给到算法实现的文件与行号，以及存放字节序的证据行。 |
| **Q3** | 分别从**官方引脚表原文**与 **BSP 的槽位映射实现**导出左槽 H2 **pin 1–12** 对应的 GPIO 集合（两条独立路径，各自给出处与行号），说明两者是否相等、集合大小是多少；再计算该集合与**板载 I²S 所用 GPIO 集合**的交集，给出交集元素与来源行号。 |
| **Q4** | 判断左槽 pin 1–12 中**哪一根 GPIO 不可用作普通按键输入**，给出理由与源码出处；并说明该引脚在主机上被驱成什么电平、由此确定对应器件的 **7 位 I²C 地址**是多少、这个地址是怎么算出来的。 |
| **Q5** | 从 BSP 读出 **eFuse 版本值 → board variant** 的映射关系（把映射的每一个分支都列出来，给文件与行号），据此说明该 variant **能否分辨** BaseBoard 丝印所写的 `V1.2` 与官方指南所称的 `1.2.1`；并说明这个结论对 `hardware/ASSUMPTIONS.md` 中与版本认定相关的假设意味着什么。 |

**Q5 所需源码不在归档快照内。** 归档 `review/chrome/D1-module-interface/evidence/bsp/` 只含 `SOURCE_INDEX.json` 所列的 12 个文件，板级初始化实现 `components/esp-mosaico-bsp/onboard/esp_mosaico.c` 不在其中。请从官方固定提交 `392860b1d1a123c3377947074b2af1f600e86c5d` 自行取得该文件，并在报告中登记你取得的文件大小与 SHA-256，说明来源 URL，以便主控核对你读的是同一份源码。其余四题所需文件均在归档内。

**所有题目都请先校验你手上的源码是不是基线本身**：`review/chrome/D1-module-interface/evidence/bsp/SOURCE_INDEX.json` 登记了各文件的哈希，请自行重算比对，不要默认归档没被改过。

## 4. 不属于本次范围（避免浪费额度）

以下**不要做**，做了也不计入本次结论：

1. **实物尺寸与上电验证。** 实物尚未到用户手中，所有机械与电气数值均为 `ASSUMPTION`。不要为了确认数值去推测实物。
2. **PCB 布局、布线与制造工艺。** 本次三项范围内没有 PCB 文件；模块板与底座主板的 `netlist` 在各自分支，不在本次范围。
3. **A0 草案与 T18。** A0 是按 V1.0 四触点＋I²C 扩展器架构画的，该架构已作废；T18 是另一条独立的基线可复现性任务。两者都不要审。
4. **方案 E／H 的取舍。** 方向已定为 D＋G，取舍过程另有记录，本次不重开。
5. **机械结构件。** `mechanical/` 尚未完成，没有可审对象。
6. **Hiro 已完成的 H0 三项**（V1.2 版本证据的置信度、方向更正是否成立、Chrome D1 左槽引脚合同）。这三项已有独立复核记录在 `review/hiro/H0/REPORT.md`，不要重审；本次审查对象是**不在** H0 范围内的那批增量。你可以读 H0 报告作为背景，但不要把它的结论当作本次任何一条的依据——它审的不是同一批文件。
7. **对外行为。** 不联系供应商、不下载需登录的资料、不发送任何消息。

## 5. 主控自述的已知弱点（请优先怀疑这些）

下列前七条是主控在 `docs/handoffs/claude.md` 第 2 节自己登记的本轮错误，第八条是结构性的。本节的用途是告诉你主控的错误模式，便于你判断哪类判断需要重查：

| # | 错误 | 谁发现的 | 现状 |
| --- | --- | --- | --- |
| 1 | 把 `MOSAICO.3mf` 当成官方整机中框，未查来源即采信，并据其内腔推整机尺寸 | 主控自己的对抗质疑轮 | 已更正 |
| 2 | 反向纠错过头：把该文件判为「未核实为官方」，理由是官方文档站没有 | 用户提供的官方评论截图 | 已二次更正。教训：官方渠道不止文档站 |
| 3 | 摄影测量用垂直方向的标定去量水平排列的焊盘间距，得出「2.54 mm 被证伪」的错误结论，并已通过用户转达给 Chrome | 主控自己的并行核查 | 已撤回（D-009-R） |
| 4 | 焊盘直径用 `(h+w)/2` 除以水平标定，混轴，偏小约 3% | **Chrome** | 已更正 |
| 5 | `git add -A` 把仍在进行的提案扫进 `main`，违反主控自定的 D-013 | 主控自己 | 已撤回 |
| 6 | **方案方向错误**：把「背面」当成唯一通路，为绕开它而引入 MCU 并据此让用户修订需求，而官方专门为扩展准备的模块槽本来一应俱全 | 主控自己的并行工作流 | 已更正 |
| 7 | 撤回误合入时未把实现补进提案分支，致该分支一度只有领取声明、没有实现 | **Chrome** | Chrome 已恢复候选并审出两处必改问题 |
| 8 | **结构性弱点：主控既是本批设计的作者，又是当前唯一持续在线者；主控对自己产出的任何核对都是同上下文核验，不构成独立复核**（`docs/DECISIONS.md` D-013、D-015、D-018）。本批产出至今没有任何独立方看过 | — | 本次 R0 就是为解决这一条而派发 |

错误 1–6 的共同模式值得注意：**先锁定一个前提，再在该前提上继续推演，新证据出现时没有回头重检前提**；以及**在自己不熟悉的测量／计算方法上给出过高置信度**。请在审查中优先怀疑属于这两类的判断。

## 6. 输出要求与交接

1. 报告写 `review/grok/R0/REPORT.md`（新建目录）。抬头按 `docs/handoffs/TEMPLATE.md` 的字段登记：任务 ID、负责人／角色、**平台与实际模型 ID 与推理档位及其核验方式**、日期、状态、领取时的 `origin/main` 提交号、输出分支与 PR、写入范围。
2. 三项范围各给 `PASS`／`CHANGES_REQUIRED`／`BLOCKED`，再给一个总结论。**证据不全不得 `PASS`。**
3. 第 3 节五道复算题**逐题单列一节**，写出过程、结果、源码文件与行号，以及与被审文件自述值的一致性比较。
4. 每条发现给：问题、所在文件与行号、为什么是问题（推导链）、影响范围、建议的处理方向。发现编号建议用 `G01`、`G02`…以便主控引用。
5. 每条结论标「实际检查／推导／未检查」。做不完的范围明确写「未检查」，不要用推测填补。
6. 报告按 `docs/handoffs/TEMPLATE.md` 补完这四节：**完成内容**（已完成的结果，不要把计划或工具启动写成验证通过）、**验证与证据**（工具版本、运行条件、检查范围、证据路径与结果；逐项区分实际检查、推导与未检查）、**未解决问题**（问题、影响范围、依据与负责人）、**接下来做什么**（明确的下一步、继续所需输入、哪些工作可并行、本会话离线后其他角色能否继续；不要写任何凭据）。
7. 报告首行沿用本包首行的声明格式，把起草者改为你自己并写上实际模型 ID，例如：`提案 · 未冻结 · 由 Grok 独立复核（<实际模型 ID> / <推理档位>）起草 · 待主控处理修改项 · 不得据以制造`。
8. 领取时与关机前各在 `docs/handoffs/grok.md` 的进展日志追加一行（`docs/TEAM_PLAN.md` 第 5 节，强制项）。该文件由你自己创建，格式可参考 `docs/handoffs/TEMPLATE.md`。
9. **关机前必须 push 并开 PR**，PR 描述记录所用模型、输入提交号、改动文件、工程依据、已做检查、未做检查与风险（`docs/TEAM_PLAN.md` 第 5 节）。不能让审查结果只留在本机，也不能承诺等待中的工具会在关机后继续执行。
10. 提交信息建议中文。不 force push，不改 `main`，不动 `review/hiro/**` 与 `review/chrome/**`。

## 完成内容

本包（`docs/handoffs/claude-to-grok.md`）与资料索引（`review/grok/R0/MATERIALS.md`）已写入分支 `docs/grok-r0-package`。所引用的仓库路径均在撰写时于 `origin/main`（`efa9739`）的工作树上核实存在；`hardware/module-board/PINMAP.md` 经 `git ls-tree` 核实存在于 `origin/claude/design-d/module-board`。本包写入不表示任何审查已开始或任何结论已成立。

## 验证与证据

- 主控为写本包实际做过的检查：在 `origin/main` 工作树上列举并打开了第 2 节所列全部审查对象文件的目录结构与章节标题；核对 `review/chrome/D1-module-interface/evidence/bsp/` 的实际文件清单，确认板级初始化实现 `onboard/esp_mosaico.c` **不在**归档内（故第 3 节 Q5 显式要求自取并登记哈希）；`git ls-tree` 核对 `origin/claude/design-d/module-board` 上 `hardware/module-board/PINMAP.md` 存在；`git log` 记录撰写时 `origin/main` 为 `efa9739`。
- 主控未做：未为本包重新复算任何校验值、GPIO 集合或镜像布局；未编译 `firmware/dock_handle/`；未运行 `mosaico_eeprom_v1.py`；未接触实物。本包不提供这些结果，正是要求 Grok 独立产出。
- 上述检查是主控的同上下文核对（D-015、D-018），**不是**独立复核，也不改变任何结论的置信度。
- **本包刻意不包含：** 第 3 节五题的答案；Hiro 在 H0 中提出的具体发现清单；对三项范围的任何提示性结论。这是为保留校准价值而做的设计选择，不是遗漏。

## 未解决问题

| 问题 | 影响 | 负责人 |
| --- | --- | --- |
| `hardware/eeprom/`、`firmware/dock_handle/`、`hardware/` 三份文档至今无任何独立方审查记录 | 这批产出目前只有作者自述可信度 | 本次 R0 的直接对象 |
| Chrome 下线后硬件技术判定权空缺，G6 阈值登记表全部待定义 | (c) 项的阈值链条无人闭合 | 用户决定 Chrome 是否有后继会话 |
| Hiro 已下线，H1–H4 规定的硬件批次复核未开始 | R0 不替代这些复核 | 用户决定 Hiro 何时上线 |
| `hardware/module-board/PINMAP.md` 仍在分支上未合入 `main` | 固件引脚表的权威来源不在基线内，一致性核对须跨分支 | 主控 |
| `docs/TASK_BOARD.md` 的 R0 登记行未写 | 任务队列记录不完整 | 主控 |

## 接下来做什么

1. 用户在 Grok 上线时告知：先 `git fetch origin --prune`，再读 `docs/handoffs/claude-to-grok.md` 与 `review/grok/R0/MATERIALS.md`。
2. Grok 领取、登记模型与推理档位及核验方式、审查、推送报告与交接、开 PR；关机前必须 push。
3. 主控收到 `review/grok/R0/REPORT.md` 后：先核对第 3 节五题的复算过程与结果，据此确定对报告其余部分的采信权重；再按各项结论更新 `docs/STATUS.md` 与 `docs/DECISIONS.md`，把必改项分派到对应子系统分支。
4. 可并行、不依赖 R0 结论的工作：主控补 `docs/TASK_BOARD.md` 的 R0 登记；模块板与底座主板分支按 `ASSUMPTION` 继续起草；用户按测量手册准备到货后的测量。
5. 本会话离线后其他角色能否继续：能。本包与索引已在仓库，Grok 不需要本会话的对话记忆；凡未写入仓库的内容一律视为不存在。
