提案 · 未冻结 · 由 Grok 独立复核起草 · 不关闭假设、不批准制造、不替代 Hiro 的 H1–H4

# G5：对 PR #56（D＋G 假设表 / ICD 到货验证勘误）的独立复核

日期：2026-09-23。角色：D-023 常设第二道独立复核（EspBot / Grok）。只读设计与他人 `review/`。本文件不是设计修订，也不把任何假设改为关闭。

## 抬头与模型核验

| 项 | 值 |
| --- | --- |
| 平台 | Cursor Cloud Agent |
| 模型 ID | `grok-4.7-high-fast` |
| 推理档位 | 同一字段中的 `high-fast`；没有单独的 reasoning 档位字段 |
| 核验 | 本会话调用 `cursor-cloud` `run-info`，读取返回的 `originalModelName`。运行 `bcId` = `bc-66600657-dd47-5eeb-a2bc-9448e0be7ac2`。这是可核验接口给出的字段（D-024） |
| 派发材料里的其他 Grok 型号称呼 | 不作为本会话的模型 ID |

## 实际冻结的提交

派发消息写有合并提交 `3c6b25cc…`。本会话在 `git fetch` 之后的对象库里找不到以 `3c6b25cc` 或 `3c6b25c` 开头的提交（`git rev-parse --verify` 退出码 128）。GitHub API 返回的 `mergeCommit` 为 `null`。本报告不复核这个不存在的对象。

PR #56 正文（API 原文）没有写出该合并 SHA。正文自述的范围与下面打开的头一致。

| 对象 | 分支 | SHA | 说明 |
| --- | --- | --- | --- |
| `origin/main` | `main` | `96fd823e93f264280207eb34b7a97e139ace7378` | PR 基线，也是本报告分支的基线。`merge-base` 与此相同 |
| PR #56 头 | `chrome/arrival-assumption-safety` | `b15a0487f49010c3efdf75fe27ee62050fb59f48` | draft，OPEN。API `headRefOid` 与 `git rev-parse` 一致 |
| 父链 | 同上 | `96fd823` → `9e5d9a0` → `e239e5e` → `ae8116e` → `b15a048` | 四次提交，线性，无合并提交 |

`git diff --shortstat origin/main...b15a048`：4 files changed, 36 insertions(+), 15 deletions(-)。与 API 的 `+36/−15`、4 个文件一致。`git diff --check` 退出码 0。

四个文件：`hardware/ASSUMPTIONS.md`、`hardware/ICD-0.2-DRAFT.md`、`docs/TASK_BOARD.md`、`docs/handoffs/chrome.md`。

GitHub `author.login` 是 `Jarvis110469`（显示名 Hiro）。四次提交的 author 都是 `chrome <chrome@chromedeMac-mini.local>`。打开 PR 不等于 Hiro 的非作者技术复核。本 G5 不代替该复核。

## 结论

| 项 | 结论 |
| --- | --- |
| PR #56 勘误本身 | **PASS**（只限验证措辞：去掉第一轮槽口卡尺接触、探针、回形针试吸、关机通断） |
| 合并建议 | **可以合入 `main`**。合并说明写明下面三条限制 |
| 30 条假设 | **全部仍为 OPEN**。关闭记录表仍是空行 |
| 尺寸 / 针位 / 连接器 / G1 / 设计草案 | **未放行**。本稿标题仍是「待 Chrome 采纳、Hiro 复核 · 不得据以制造」。ICD 第 10 节仍是「未冻结」「非 G1/G2/G3 放行」 |

三条必须写进合并说明的限制：

1. AS-01 至 AS-30 的状态列保持 `OPEN`。照片、估算和本勘误都不关闭假设。
2. 不冻结几何、针位、连接器选型或 G1，不采纳 Claude 的 D＋G 设计草案。
3. 不替代 Hiro。没有实物测量。

G5-04、G5-05 是残留措辞，不把已删掉的危险步骤加回去。为了这两条挡住本 PR，会让 main 上的回形针、探针和关机通断句子继续留着。若要在同一次提交里收紧，可按 G5-04、G5-05 改两处，再合。

## 本轮独立做了什么

数字来自本会话对 `b15a048` 的解析，不是照抄 PR 正文。

1. `gh pr view 56` 读取头、基线、`mergeCommit`、草稿状态、正文与文件列表，再 `git fetch` 该分支并 `git diff origin/main...b15a048`。
2. 解析 `hardware/ASSUMPTIONS.md` 第 1 节：表头 8 列，数据行 30，编号 AS-01 至 AS-30 连续，每行 8 列，状态列均为 `OPEN`。与基线逐列比较：假设、使用文件、若错影响、状态四列无差异；变化只在 AS-02、AS-03、AS-04、AS-05、AS-06、AS-17、AS-24 的验证步骤，以及其中部分行的器材、负责人。
3. 解析 ICD 第 5 节 ME-D 表：表头 4 列，ME-D-01 至 ME-D-11 共 11 行，每行 4 列。约束列与假设编号列相对基线无差异。验证列只改 ME-D-01～06 与 ME-D-08。
4. 在这两份头文件中检索：卡尺、探针、曲别针、回形针、通断、导通、深度尺、深度杆、试插、试吸、万用表、插入。曲别针 0 处。回形针、探针、试吸只出现在禁止句。通断出现在新的边界说明、AS-17 的「须另制专项步骤」，以及未改的 EL-D-05「G6-A6 通断」。
5. 对照基线上的 `docs/MEASUREMENT_PROTOCOL.md` 第 0～5 节（M06「小孔不塞量爪」、4B.3 的 Q06/Q07/Q08、第 7 节通断留待办）。只对 draft PR #55 的头做了照片编号交叉：`82df4c65`，未审其记录表全文。

未做：没有改假设、ICD、规程或任务板；没有测量实物；没有把 #55 的表单审查并进本结论；没有宣称 EDA、DRC 或工厂结果。

## 发现

### G5-01　信息　可核验的头不是派发消息里的合并 SHA

实际打开的是 draft PR #56 头 `b15a0487f49010c3efdf75fe27ee62050fb59f48`，基线 `96fd823e93f264280207eb34b7a97e139ace7378`。`mergeCommit` 为空。`3c6b25cc` 不在对象库中。后续讨论以这个头为准。

### G5-02　已核　30 条假设仍全部 OPEN，假设正文未改

第 1 节 30 行的状态列都是 `OPEN`。第 4 节关闭记录只有一行「（空）」。相对基线，假设句子、影响和使用文件没有改字。AS-17 的负责人改为「用户拍照、Chrome/Hiro 后续复核」，这是验证分工的写法，Hiro 并未因此完成本项复核。

状态说明里的 `MEASURED` / `CLOSED` / `FALSIFIED` / `SUPERSEDED` 仍是取值定义，没有数据行使用它们。

### G5-03　已核　ME-D 十一行列数一致，冻结登记未动

ME-D-01～11 均为 4 列，假设编号仍指向 AS-01、AS-02、AS-03、AS-04、AS-05、AS-06、AS-22、AS-24、AS-25、AS-08、AS-12。约束正文未改。第 10 节冻结表仍是「尚未复核 / 非 G1/G2/G3 放行 / 未冻结」。文件首行仍是「待 Chrome 采纳、Hiro 复核 · 不得据以制造」。新加段落写明「未采纳或冻结本稿」。

被改掉的旧指令（基线对照，头上已不存在为肯定句）：

| 位置 | 基线上的到货动作 | 头上的写法 |
| --- | --- | --- |
| AS-02 / ME-D-02 | 卡尺量针中心距与 pin 1→19 跨距；排针作比例尺 | Q07/Q08 照片与远离设备校准的标尺；不以卡尺接触孔位，不把排针插入 |
| AS-03 / ME-D-03 | 卡尺/深度尺量凹凸 | 卡尺只量绝缘外壳外表面；无检具则凹深「未测」 |
| AS-04 / ME-D-04 | 卡尺量 pin 1/20 到外壳 | 照片标注；不以卡尺/深度杆进入接口；针号未证实则「未测」 |
| AS-05 / ME-D-05 | 回形针试吸；绝缘探针试插入深度 | 外观照；不要回形针/磁铁试吸，不把探针插入 |
| AS-06 / ME-D-06 | 候选公头只试插一次 | 第一轮不插拔；试插留到独立步骤与夹具复核之后 |
| AS-17 / 第 2 节 | 关机后万用表通断，并把 USB-C 外壳当作 GND | 第一轮只拍照；通断另制步骤；关机不等于断电；不预设 USB-C 外壳为 GND |
| AS-24 / ME-D-08 | 试插后手感 | 第一轮只看可见锁扣并拍照；不为取证插拔原装模块 |

### G5-04　低　「两文件已无第一轮肯定性卡尺指令」这句自述过宽

槽口接触、探针、回形针、关机通断这四类肯定句，在两份头文件里没有留下。下面两处仍是肯定的到货测量，PR 正文的检索句把它们也说成没有：

- AS-01 与 ME-D-01 仍要求第一轮按规程第 3 节用卡尺量外壳 W/H/T。这与规程第 3 节以及本勘误新加的「安全外表面」边界一致。ME-D-01 另写「未称不关闭 AS-01」；AS-01 器材列仍把电子秤和称重放在同一步，少了这句限制。
- AS-22 与 ME-D-07 本 diff 没有改。AS-22 仍写「到货量 USB-C 中心 X/Z 与开口尺寸」，器材只有卡尺。规程 M06 对 USB-C 等开孔是旁边放标尺、小孔不塞量爪。按 ASSUMPTIONS 这一格单独执行，量爪可以读成伸进开口。这是基线上的旧句，不是本勘误新加的危险步骤。

### G5-05　低　表前旧句仍把每一行都说成到货可执行

`hardware/ASSUMPTIONS.md` 在新勘误段之前仍有：「本表每条给出到货后可执行的验证步骤与器材。」同一页新段又写：后续夹具、试插、上电、通断和 G6 不是用户到货即可执行的动作。G6 各行（例如 AS-09、AS-11、AS-17 所推迟的通断、EL-D-05 的 G6-A6）本轮没有改写。新段足以拦住只读新段的人；只读旧句的人仍会把 G6 当成到货动作。

### G5-06　低　三视图编号依赖尚未合入的 PR #55

头上的 AS-02/03/05 与 ME-D-02/05 要求 Q07/Q08 的正视、斜视、定位，以及 P00d/Q06。基线规程里 P00d、Q06、Q07、Q08 已有定义，但 Q07/Q08 各是一张接口特写，没有 `_正视` / `_斜视` / `_定位` 后缀。这些后缀出现在 draft PR #55 头 `82df4c6534c3aa534e5ec67381e4a7767708c749` 的规程第 4B.3 节。本审没有审 #55 的记录表。先合 #56 不会恢复探针或通断；在 #55 或同等文字进入 main 之前，假设表要求的文件名比现行规程更细。

## 与 T14 第一轮的对照

基线 `docs/MEASUREMENT_PROTOCOL.md` 第 0～5 节允许：认版照片、卡尺量外壳（第 3 节）、标尺放在旁边且小孔不塞量爪（M06）、V1.2 侧槽只拍照不插拔（4B）。第 7 节把通断留到电池隔离和 Hiro 复核之后，并写明关机不是断电、USB 外壳先不当信号 GND。

本勘误把 AS-02/03/04/05/06/17/24 和对应 ME-D 行改到这个边界内。AS-01 的外壳卡尺本来就在第 3 节里面。G5-04 的 USB-C 开口是唯一仍比 M06 松的到货格。

## 明确不构成的结论

- 全部假设仍 OPEN。
- 不构成外形尺寸、孔距、针号、母座型号或 G1 放行。
- 不构成设计采纳，不替代 Hiro，不批准制造或上电。
