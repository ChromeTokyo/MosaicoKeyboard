# Chrome 后续工作顺序与验证出口

2026-09-20，输入A0 `3d5a112`、T02证据提交`3d8ad4c`／[PR #4](https://github.com/ChromeTokyo/MosaicoKeyboard/pull/4)。T02完整性已由D-011确认；技术结论仍CHANGES_REQUIRED。这里是执行顺序和关闭条件，不是修复完成声明。

1. **先交T14规程与统一接口草案。** 到货前让用户知道测什么、怎么留证；同批给出A/B条件与G3设计要求。本轮承担此项。
2. **F03先修：建立可复现、失败可见的验证基线。** 给输入缓存／生成器／输出逐份哈希与清单；缺失／失败缓存须显式报错，研究占位只能显式选择并阻止生产BOM导出。12个变化位号逐一处理，C45783、U8封装及R26缺缓存不得被“重跑成功”自动确认。两次隔离重建及仓库受控输出比对，说明唯一源和导出从属关系。原A0不被覆盖为历史正确版本。Hiro是原A0生成者，其当时输入说明仍待补；不伪造历史来解除U-11。
3. **F01第二：消除误连并用实际EDA网表证明。** 在T05原A0审查记录之后、T07修订范围内调整生成器布局／连线。若设计网络未变，预期43网、242逻辑引脚逐一对应，特别确认U4.3/U4.6/C11.1/J3.3/TP6.1不再落入BOOST_SW。不是只看JSON网络名或导线视觉分开；若T07有拓扑变化须另列批准差异。
4. **F02第三：从有效最小PCB文件逐项定位格式。** 取实际编辑器产生、可重开导出的最小原生样例，分离测试LIB层／身份字段、对象ID、PAD字段、DRCRULE等候选；每次只改一个维度并留结果，不能把某异常说成唯一根因。成功导入后还要编辑、保存、重开、真实导出／文件获取与网络／封装检查。点击下载按钮不等于已取得文件。
5. T03参数化CAD、T04其余原厂／供应证据、T05原A0电气审查继续；T07整理修订和Hiro H1包。F02最小有效样例和T04资料可在F03期间并行研究，但不能让它们的通过状态遮蔽未固定的输入。

保留脚本作为当前实验生成唯一源，暂不手改五个生成产物。是否转为EDA原生权威源须另写版本化切换决定，脚本降为只读导入或验证用途，不能两套并存各自修改。

## PR #4 合入前卫生检查

D-011要求先同步main，保留主控新任务和Hiro交接。当前PR #4仍未合并，原分支保持`a6db65a`；本轮不会偷偷改写它的证据或旧交接。

2026-09-20只读核查：GitHub报告PR #4 `CONFLICTING/DIRTY`，确需处理文档冲突。实际PR三点差异清单没有删除`docs/handoffs/hiro.md`；“旧分支树中没有该文件”本身不等于普通三方合并必然删除。**仍执行主控要求的同步与保留检查，不以这个区别绕过合入门槛。**

后续同步应保留本轮最新任务板、主控派发包、Hiro交接和Chrome追加进展；A0源、T02原始证据及既有SHA256不变。同步后检查相对main不存在意外删除、核对Hiro交接blob和主控状态、重验T02证据哈希，再交主控集成。主控拥有合入权；Hiro复核按既定窗口，不在本轮唤醒或冒充。
